---
name: tailor-resume
description: Tailor one resume for one job description with callback.
---

Use the `tailor-resume` skill. Run one full JD through callback with `load_jd`, `submit_keywords`, optional `get_wiki_pages`, and `submit_tailor`. Do not scan job boards, update ledgers, or submit applications.

ARGUMENTS: $ARGUMENTS
