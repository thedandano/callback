---
name: setup-job-automation
description: Set up or update recurring callback job lead scanning automation.
---

Use the `setup-job-automation` skill. Update or create a scan-first Codex automation that runs `auto-job-apply` by default and preserves the explicit approval boundary for applications.

ARGUMENTS: $ARGUMENTS
