---
name: onboard-profile
description: Register a resume and compile the callback profile wiki.
---

Use the `onboard-profile` skill. Register the user's resume with callback, optionally ingest skills/accomplishments files, call `onboard_user`, then call `compile_profile`. Do not tailor a resume in this command.

ARGUMENTS: $ARGUMENTS
